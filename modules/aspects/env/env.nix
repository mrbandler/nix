# Session variables ride with the shell, so they feed the cli aspect.
{
  den.aspects.cli.homeManager.home.sessionVariables = {
    TERMINAL = "wezterm";
    EDITOR = "hx";
    VISUAL = "zeditor";
    PAGER = "bat --style=plain";
    BROWSER = "zen-beta";
  };
}
